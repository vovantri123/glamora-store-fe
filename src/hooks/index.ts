// Export all auth-related hooks
export { useAuthGuard, useGuestGuard } from './useAuthGuard';
export { useImageWithFallback } from './use-image-with-fallback';
export { useIsMobile } from './use-mobile';
